import ChatInterface from '@/components/chat/ChatInterface';

const Index = () => {
  return (
    <div className="relative">
      <ChatInterface onSourcesUpdate={() => {}} />
    </div>
  );
};

export default Index;
